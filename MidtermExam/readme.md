1. implement TCP/UDP dual protocol supported chat server/client (client try tcp first, and automatic fall back to UDP if TCP connection fails)
2. make the TCP/UDP dual protocol supported chat server that can receive message from both TCP and UDP clients, and broadcast every received message to the online TCP/UDP clients.
3. implement the TCP/UDP dual protocol supported chat client that can send message to the TCP/UDP dual protocol supported chat server, and can receive message from the TCP/UDP dual protocol supported chat server.
4. The TCP/UDP dual protocol supported chat server/client should be able to handle multiple clients concurrently.
5. The TCP/UDP dual protocol supported chat server/client should have a simple command-line interface for sending and receiving messages.